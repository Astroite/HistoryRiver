# M1-01 工作日志

> 追加原则：每完成一个 Task，记录结果、实质改动、验证、阻塞和后续影响。不得用改写旧记录的方式隐藏失败或决策变化。

## 2026-07-16｜迭代启动

### 决策

- 启动唯一活跃迭代 `M1-01`，主题为“核心体验原型”。
- 长期基线正式拆分为体验设计 `docs/design.md` 与技术设计 `docs/tech.md`。
- P0 使用小规模、确定性的模拟数据验证空间语义，不等待完整史料数据。
- 本轮验证聚焦 D-01 至 D-06、T-01 至 T-03，不扩展账号、移动端、正式内容和生产部署。

### 结果

- 已创建 `current/README.md`、`plan.md` 与 `work-log.md`。
- 已定义五个独立 Task、逐项完成条件、需求—验证追踪和风险边界。

### 验证

- 计划包含任务上下文、执行范围、依赖和可执行验收，符合 `docs/iterations/README.md` 的讨论阶段契约。
- 当前只有一个 `current/` 活跃迭代；目标归档位置已声明但尚未提前创建归档副本。

### 阻塞

- 无。

### 下一步

- 执行 `M1-01-T01`：拆分并升级体验设计与技术设计基线。

## 2026-07-16｜M1-01-T01 完成：双文档与迭代基线

### 结果

- `docs/design.md` 从“概念设计 v0.1”升级为“体验设计基线 v0.2”，保留全部已确认产品结论。
- 新建 `docs/tech.md`，承接坐标、时间表示、状态机、数据合同、渲染层、性能预算、质量降级与工程验证。
- 两份主文档均链接当前迭代；迭代计划建立 D-01 至 D-06、T-01 至 T-03 的需求追踪。
- 技术基线暂定 TypeScript、Vite 与 Three.js `WebGLRenderer`；具体版本只在工程初始化时锁定，WebGPU 仅作为后续实验。

### 实质决策

- 设计文档是历史语义和用户体验的来源；技术文档不得自行改变视觉元素所代表的历史含义。
- 当前迭代的过程数字与临时假设不直接进入长期基线，必须先完成验证。
- 性能降级先牺牲后期效果、像素比和装饰密度，不能删除当前窗口、选中人物、事件、思想关系、时间方向或返回路径。

### 验证

- 检查 6 个 Markdown 文件：全部存在。
- 扫描本地 Markdown 相对链接：0 个缺失目标。
- 检查 Markdown 围栏：0 个未闭合代码块。
- `design.md` 737 行、75 个标题；`tech.md` 554 行、47 个标题。
- 原文中移出的技术待决项已在 `tech.md` 第 14 节承接；原“下一阶段建议”已转换为当前迭代的可验收 Task。

### 阻塞

- 无。

## 2026-07-16｜M1-01-T02 完成：P0 体验合同

### 结果

- 在 `design.md` 定义 S0 观河、S1 入流、S2 观事、S3 逐人、S4 溯源五个核心状态。
- 为每个状态写明画面目标、用户动作、系统反馈、进入/退出条件和失败信号。
- 冻结一版 75 秒默认导览节拍，并规定任何主动接管都会停止时间表推进。
- 定义人物光点、人生悬丝、事件点云、思想丝线、二十年窗口与抽象山河的不可删除视觉特征。
- 建立 7 项无讲解用户验证任务和量化阈值，要求至少 5 位接近目标用户的参与者。
- `tech.md` 对应建立状态机、统一投影、确定性夹具、设计测试入口和 D/T 追踪表。

### 验证

- 每个核心状态均有可观察失败信号，不依赖实现者主观评价。
- D-01 至 D-06 均能追踪到具体技术责任和验证证据。
- 导览、用户接管、低动态与一键返回路径均在设计和技术两侧形成合同。
- 用户验证阈值与迭代 `plan.md` 的验收要求一致。

### 阻塞

- 无。真实设计观察与性能数据需等待 `M1-01-T03` 可运行原型，不能在当前阶段提前标记通过。

### 下一步

- 执行 `M1-01-T03`：按 `tech.md` 工程进入条件建立前端工程，并先完成 S0→S1→S2 的最短同源三视角切片。

## 2026-07-16｜T01/T02 文档验收复核

### 验证

- 共检查 6 个 Markdown 文件，所有相对链接目标存在。
- 带锚点的跨文档链接均能匹配目标标题：0 个无效锚点。
- 所有代码围栏成对闭合：0 个未闭合围栏。
- 复核 10 项首轮确认关键词：目标用户、审美目标、完整历史范围、二十年窗口、完整人生、思想传承、人民汪洋、无旁白与诸子百家等均仍在 `design.md`。
- D-01 至 D-06、T-01 至 T-03 在设计、技术或迭代计划中共形成 19 处追踪引用。

### 结论

- `M1-01-T01` 与 `M1-01-T02` 验收通过。
- 当前迭代不归档；`M1-01-T03`、T04、T05 仍待执行。
